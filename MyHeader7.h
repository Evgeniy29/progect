#pragma once // include guard — защита от повторного включения заголовочного файла
namespace MyLib
{
	void printArray(const float array[], int size); // Прототип функции
	void podschet(const float array[], int size);
}